{
  "name": "pitomnik-myta-backend",
  "version": "1.0.0",
  "description": "Backend API for Pitomnik Myta",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon src/index.ts",
    "build": "npx --package=typescript tsc",
    "start": "node dist/index.js",
    "migrate": "node dist/database/migrate.js",
    "postinstall": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.2"
  }
}
